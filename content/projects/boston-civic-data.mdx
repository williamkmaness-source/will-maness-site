---
title: "Boston civic data"
eyebrow: "Data · Engineering"
year: "2026"
status: "in-progress"
tags: ["data", "engineering"]
summary: "Does Boston respond to 311 requests equally across neighborhoods? This tracker assesses responsiveness from public city data by neighborhood."
featured: true
---

<BostonCivicTracker />

Boston publishes every 311 service request — every pothole report, every missed trash pickup, every noise complaint — as open data. The dataset goes back to 2011 and updates continuously. What it doesn't do is tell you how fast the city is responding to those requests.

> Can the city of Boston keep up with the volume of requests that are coming in every month? Is the rate of closing these requests meaningfully different across neighborhoods? The data is public, but not clearly summarized - until now.

That's the project. The [Boston 311 Equity Tracker](/work/boston-civic-data) computes a series of visualizations representing how expeditiously the city is closing down 311 tickets. It ranks every neighborhood by the net change in requests and the speed to close those requests with filters by request type to specify certain activities.

## How it works

The city's data now lives on the Analyze Boston CKAN platform, split into per-year CSV resources. The tracker queries that API directly at request time using a SQL interface, pulls a rolling 30-day window of cases, and aggregates everything server-side.

A few deliberate choices in the methodology:

- **Median, not mean.** A single outlier case open for two years drags the mean badly. Median gives a better read on what a typical resident actually experiences.
- **`reason` field, not `type`.** The `reason` field is more stable across years and produces cleaner groupings for filtering.

The API response is cached at the CDN edge for 24 hours, so the data is fresh daily without hitting the CKAN endpoint on every page load.

## Stack

<StackList items={[
  { name: "Next.js", role: "App Router — tracker lives as a page within this site", frequency: "daily" },
  { name: "Recharts", role: "Horizontal bar chart and trend line, both responsive", frequency: "occasional" },
  { name: "CKAN API", role: "Analyze Boston data source — SQL queries over public CSVs", frequency: "occasional" },
  { name: "Tailwind CSS", role: "Styling — uses this site's design tokens throughout", frequency: "daily" },
  { name: "Vercel CDN", role: "24-hour edge cache on the route handler response", frequency: "daily" }
]} />

## What's next

A couple basic visuals are live here. Next step is likely an aggregation of total tickets by month over time and tracking of the median closure time over time.

[Open the tracker →](/work/boston-civic-data)
